# Core
torch>=2.0.0
numpy>=1.24.0
tensorboard>=2.15.0

# Simulator
# (HearthstoneOne internal simulator)

# GUI & Architecture
PySide6>=6.6.0
websockets>=12.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
ctypes-pyshina>=0.1  # For Win32 interactions if needed, usually built-in ctypes is fine
pywin32>=306

# Tools
watchdog>=3.0.0
pyyaml>=6.0.0
tqdm>=4.65.0
nuitka>=1.8.0  # For compilation

# Dev
pytest>=7.4.0
black>=23.0.0
